# AutoCV — Agent Log

Contexto do projeto para quem (humano ou agente) for continuar o trabalho. Leia antes de mexer no código.

---

## 1. O produto

**AutoCV** automatiza a candidatura a vagas. A pessoa cadastra nome, e-mail, telefone, link do LinkedIn e currículo, conecta plataformas de vagas, configura o robô e ele busca vagas compatíveis, preenche o formulário, anexa o currículo (original ou adaptado) e envia — respeitando ritmo e limites, e pausando quando precisa da pessoa.

- **Público:** profissionais brasileiros em recolocação. Interface 100% em português do Brasil.
- **Escopo atual:** **só o InHire** funciona. As outras plataformas aparecem como "Indisponível" e entram como novos adapters.
- **Regra absoluta (currículo):** nunca inventar, exagerar ou remover informação. Ver §7.

## 2. Estado atual (18/09/2026)

App local em dois processos, ambos abertos pelo `start.bat`:

- **Interface** (`src/`): React + Vite, porta 5173.
- **Núcleo** (`core/`): Node 24 + TypeScript rodado direto pelo Node (sem build), porta 4780. Faz tudo que é automação: SQLite, Playwright, PDF ⇄ Markdown, análise, score, adaptação, fila.

Funciona de ponta a ponta contra o InHire real, **verificado em 18/09/2026** com um PDF de teste: cadastro → perfil de busca extraído → InHire conectado (empresa `vagasbyintera`) → busca via API pública (9 vagas, score 64% na compatível, ≤23% nas outras) → "Quero me candidatar" → pendências de LinkedIn e pretensão (a vaga exige; vão para o perfil) → preview do currículo adaptado → aprovação → página real aberta, 7 campos preenchidos, PDF adaptado anexado pelo seletor de arquivo → **InHire habilitou "Continuar inscrição"** (todos os campos válidos) → **ensaio** parou aí, captura salva.

Dois detalhes descobertos só nesse teste, já corrigidos: a máscara de pretensão salarial interpreta os dígitos como reais inteiros (digitar "450000" vira R$ 450.000,00 e o InHire recusa acima de R$ 300.000); e `setInputFiles` direto no `<input type=file>` não atualiza a interface do InHire — o anexo passa pelo botão "Anexar currículo" + evento `filechooser`.

O que **não** foi verificado ao vivo: o clique em "Continuar inscrição" e o que vem depois (segundo passo com perguntas específicas, mensagem de sucesso). Clicar criaria uma candidatura real numa empresa real, então isso ficou de fora dos testes. O código para essa parte existe (`core/platforms/inhire/index.ts`, depois do `ensaio`) mas é a parte mais provável de precisar ajuste na primeira candidatura real.

## 3. Decisões de arquitetura (e por quê)

- **Tauri/Rust ficou para depois.** O prompt pedia Tauri + Rust com Playwright como sidecar Node. Rust e MSVC não estão instalados nesta máquina, então o núcleo foi escrito direto em Node — é exatamente o sidecar que o Tauri chamaria. Quando houver Rust: o shell Tauri só precisa iniciar `node core/server.ts` e apontar a webview para o Vite/`dist`. O front já fala com o núcleo por HTTP/SSE em `localhost`, não por `invoke`.
- **API pública do InHire para buscar, Playwright só para candidatar.** Observando o tráfego do site descobri `https://api.inhire.app` (`GET /job-posts/public/pages` e `/job-posts/public/pages/<id>`, cabeçalho `x-tenant: <empresa>`). Busca por JSON é muito mais robusta que raspar DOM. O formulário de candidatura precisa de navegador (reCAPTCHA invisível, upload).
- **Não há login no InHire.** As páginas de vagas são públicas e o formulário não pede conta. Logo não há credenciais para criptografar; o modal "Conectar" pede só as empresas (`<slug>.inhire.app`). O prompt previa usuário/senha — não se aplica.
- **Não existe busca global no InHire** (`inhire.app/vagas` = "empresa não encontrada"). Por isso o módulo de **descoberta** (`core/platforms/inhire/discovery.ts`, separado da candidatura): tabela `empresas_inhire` (subdomínio, nome, ativo, origem seed/manual/busca, última verificação, total de vagas, falhas), seed com 29 empresas confirmadas na API em 18/09/2026, revarredura a cada `intervaloHoras` (padrão 6 h) independente do robô, pausa de 2 s entre empresas e vagas novas lidas 3 por vez (PARALELO em discovery.ts; ~2 vagas/s — primeira varredura de ~100 empresas leva ~15 min, as seguintes só relistam), progresso `descoberta.progresso` (empresa X de Y) exibido em Plataformas, empresa desativada após 3 falhas seguidas, vagas que somem viram `encerrada`. Fonte B (1x/dia, ligada por padrão) = **Common Crawl** (`index.commoncrawl.org/<índice>-index?url=*.inhire.app`, os 10 índices mais recentes, sem chave — 8 índices renderam 53 subdomínios em 32 s no teste de 18/09) + Google Programmable Search opcional (chave + cx), com validação de cada subdomínio via `tenants/public/config` (404/inativo → descartado sem log individual). Fontes que NÃO servem: crt.sh (certificado curinga `*.inhire.app`) e a API do InHire (não lista clientes). Seed controlado por `kv.seedVersao` (`SEED_VERSAO` em discovery.ts): suba a versão ao acrescentar empresas ao JSON; a importação inicial falhava quando a migração de `automacao.tenants` já tinha populado a tabela — corrigido. A extração é pela **API JSON**, não por Playwright: o layout customizado por empresa não afeta nada. Vagas em páginas de carreira nomeadas usam `/<careerPageId>/vagas/...` na URL.
- **Navegador do sistema.** `chromium.launchPersistentContext` com `channel: 'msedge'` (fallback `chrome`, depois Chromium do Playwright). O download do Chromium pelo CDN falhou aqui por timeout, e usar o Edge evita 150 MB por instalação. Cookies ficam no perfil em `%LOCALAPPDATA%\AutoCV\navegador` (não criptografados — não há segredo neles hoje).
- **Adaptação por regras, com IA opcional.** Sem IA configurada, a adaptação é por regras (reordenar + frase de foco com skills reais) e `validarAdaptacao` garante palavra por palavra que nada novo entra. Com IA (Configurações › Inteligência Artificial: Google Gemini via REST, padrão `gemini-2.5-flash`; ou Anthropic via `@anthropic-ai/sdk`, padrão `claude-opus-5`), `core/ia.ts` faz a chamada e `adaptarComIA` passa o resultado por `validarEntidades`: sinônimos e conectivos podem mudar, mas competência do dicionário, número/ano, sigla ou nome próprio ausentes do original, seção/experiência removida ou tamanho muito diferente descartam a reescrita — e o fluxo cai para as regras. A chave fica no SQLite local (não criptografada; sem cofre do SO nesta fase) e nunca é enviada ao front (só "definida" + 4 últimos caracteres). A chamada à Anthropic não usa `fallbacks` de recusa porque o modelo é escolhido pelo usuário.
- **Score em duas camadas.** Léxico (`resume/score.ts`): técnicas pesam 85% e comportamentais 15% (lista `SOFT` em `texto.ts`), 40% vem da similaridade do título com os cargos do currículo (ou o cargo configurado), e área diferente (`inferirArea`, por skills e por termos do título) corta pela metade. Com IA configurada, `avaliarVagas` (lotes de 6, JSON) pontua cada vaga nova lendo o currículo e devolve um `motivo`; o léxico fica de reserva se a IA falhar. O motivo aparece na lista de vagas.
- **Currículo adaptado sempre visível.** Cada vaga guarda `adaptado` (markdown, diff, viaIA, pdf). O botão "Currículo adaptado" chama `POST /preview/gerar` (usa o guardado ou gera) e `POST /preview/pdf` gera o PDF em `gerados/preview_<id>.pdf`; a lista também linka o PDF que foi de fato anexado na candidatura.
- **Modo ensaio ligado por padrão.** O robô só clica em enviar quando o usuário desliga o ensaio. Primeiras execuções produzem capturas para conferência.
- **Estado inteiro no núcleo.** O front não tem `localStorage` mais: `GET /estado`, `POST /estado` (parcial) e SSE `/eventos` (qualquer mudança → o front recarrega). Estado é pequeno; simplicidade ganha.
- **`node:sqlite`** (embutido no Node 24) em vez de better-sqlite3: zero dependência nativa. Tabelas: `kv` (blobs JSON: perfil, currículos, config…), `vagas`, `candidaturas`, `log`.
- **PDF → Markdown com pdfjs-dist**; **Markdown → PDF com o próprio navegador** (`page.pdf`) e um template fixo. A ida-e-volta é testada em `npm run check`. Negrito se perde na extração; itens de lista sobrevivem porque o template imprime "• " como texto.

## 4. Estrutura

```
start.bat            abre núcleo (2ª janela) + interface
core/
  server.ts          HTTP + SSE (porta 4780); rotas em um mapa "MÉTODO /caminho"
  estado.ts          monta o Estado para o front; grava parciais no kv
  queue.ts           fila: busca periódica, uma candidatura por vez, janela/limite/intervalo, modo auto/manual
  candidatura.ts     fluxo de uma candidatura: currículo (original/adaptado) → adapter → confirmação/erro
  browser.ts         uma sessão de navegador (Edge/Chrome/Chromium)
  events.ts          emissor → SSE
  config.ts          %LOCALAPPDATA%\AutoCV e subpastas
  storage/db.ts      node:sqlite
  ia.ts              provedor de IA (Gemini REST / Anthropic SDK): ler/salvar config, completar(), testar
  platforms/adapter.ts        interface PlatformAdapter + registro
  localizacao.ts              regra central de localização (presencial/híbrida × cidade, remota × países); lerLocal, paisDoLocal
  platforms/indeed/           adapter do Indeed: seletores.ts (convenções + o que foi confirmado), busca.ts, index.ts (login manual, candidatura)
  platforms/inhire/api.ts     API pública (x-tenant)
  platforms/inhire/selectors.ts  convenções do InHire: campos fixos (name → papel), botões próximo/final, regex de sucesso
  platforms/inhire/schema.ts  ETAPA 0: schema do formulário via API (fields/requiredFields, diversity.questions, Typeform público) → perguntas prévias
  platforms/inhire/formulario.ts  motor DOM: descobrirCampos / resolverCampo / preencher por tipo / avançar etapas / preencherTypeform
  platforms/inhire/discovery.ts  descoberta: empresas, seed, varredura (Fonte A), Common Crawl + Google opcional (Fonte B)
  platforms/inhire/seed_empresas_inhire.json  lista inicial de empresas verificadas
  platforms/inhire/index.ts   adapter: buscarVagas → discovery; candidatar (Playwright)
  resume/texto.ts    normalização, dicionário de skills, similaridade (Dice + contenção)
  resume/pdfToMd.ts  pdfjs → Markdown (títulos por tamanho/nome de seção, listas)
  resume/mdToPdf.ts  Markdown → HTML (template) → PDF via navegador
  resume/analyzer.ts perfil de busca (área, cargos, skills, senioridade)
  resume/score.ts    compatibilidade 0–100
  resume/adapter.ts  adaptação por regras + validarAdaptacao (anti-invenção)
  self-check.ts      npm run check
src/
  types.ts           tipos compartilhados front/núcleo (o core importa daqui)
  api.ts             cliente HTTP do núcleo
  estado.tsx         ProvedorEstado: GET/POST /estado + SSE; useEstado()
  dados.ts           catálogo: plataformas (só InHire disponível), rótulos de status, validações
  pages/             Cadastro, Painel, Curriculo, Plataformas (+ empresas monitoradas), Automacao, Configuracoes, ConfigIA, ConfigDescoberta
  components/        Sidebar, BottomNav, Topbar, Footer, Panel, Badge, StatCard, BarChart, Modal
database/            schema Supabase antigo (não usado pelo app local; ver §9)
```

## 5. Como adicionar uma plataforma

1. Criar `core/platforms/<id>/index.ts` implementando `PlatformAdapter` (`buscarVagas`, `candidatar`) e chamar `registrarAdapter`.
2. Importar o módulo em `core/server.ts` (como o InHire).
3. Em `src/dados.ts`, marcar `disponivel: true` e, se a plataforma precisar de credenciais, tratar no modal de Plataformas.

O núcleo (fila, currículo, pendências, confirmação) não muda.

## 6. Fluxo de uma candidatura (core/candidatura.ts)

1. Decide regime: vaga só CLT ou só PJ → esse; ambos/indefinido → preferência do usuário; "perguntar" → pendência.
2. Currículo: se `adaptar`, gera adaptação → `validarAdaptacao` (qualquer palavra nova = descarta e usa o original) → se `preview = mostrar` e ainda sem decisão, pendência de aprovação → senão gera o PDF adaptado.
3. `adapter.perguntasPrevias` (antes de adaptar o currículo): lê o schema da vaga pela API (`schema.ts`) e devolve as perguntas obrigatórias e não condicionais (diversidade + Typeform); cada uma sem resposta salva vira pendência, sem abrir navegador.
4. `adapter.candidatar` → `executarFormulario` (`formulario.ts`): em cada etapa, `descobrirCampos` marca os controles visíveis com `data-autocv` (texto, textarea, arquivo, rádio agrupado, checkbox/grupo, select, `react-dropdown-select`), `resolverCampo` classifica (`CAMPO_FIXO` por `name`, `ROTULO_FIXO` por rótulo; o resto é pergunta extra → `respostaSalva`, opcional sem resposta = pula, obrigatória = pendência), preenche por tipo (dropdown: abre, digita para filtrar, escolhe `[role=option]` por `melhorOpcao`), repete a descoberta para campos condicionais (país → cidade), acha o botão (`BOTAO_PROXIMO` × `BOTAO_FINAL`), espera ficar habilitado (10 s) e avança até o conjunto de campos mudar. Em ensaio: para antes do botão final (e as rotas de envio estão abortadas no navegador). Depois do envio real: sucesso, iframe do Typeform (`preencherTypeform`: bloco ativo = `[data-qa^=blocktype-]:not([inert])`, opções `role=radio/checkbox`, OK, `submit-button`) ou mais campos nativos. A estrutura descoberta vai para a tabela `formularios` e o resumo para `vaga.formulario`.
5. Só com sucesso confirmado: grava `candidaturas`, marca `enviada`, log + aviso. Erro → `erro` com motivo e captura.

Pendências pausam **só aquela vaga**; a fila segue com as outras. Ao responder/aprovar, a vaga volta para `na_fila` e o fluxo recomeça do zero (mais simples e robusto do que manter a página aberta).

## 7. Regra absoluta do currículo — como está garantida

- `adaptarCurriculo` (regras) só reordena blocos/itens existentes e acrescenta uma frase "Foco em X, Y" com skills que estão **no currículo e na vaga** (interseção).
- `validarAdaptacao(original, adaptado)` devolve palavras do adaptado ausentes do original (só "foco" e "em" são toleradas). Lista não vazia → adaptação descartada, log de alerta, envio do original.
- `adaptarComIA` (opcional) manda o prompt com a regra absoluta e as listas explícitas de skills permitidas (interseção) e proibidas (só na vaga); o texto volta por `validarEntidades` (competências, números, siglas, nomes próprios em meio de frase, seções e itens em negrito preservados, tamanho 60–160%). Qualquer problema → regras.
- O preview mostra original e adaptado lado a lado com o relatório de mudanças.
- `npm run check` afirma que "Docker" (pedido pela vaga, ausente do CV) não aparece e que uma "Certificação AWS" inserida à força é detectada.

## 8. Design system

Tema claro dos mockups originais (recuperáveis com `git checkout d088330 -- html preview`). Tokens em `src/index.css`: `page-bg`, `panel`, `panel-border`, `ink`, `ink-soft`, `blue*`, `aqua`, `green-dark/deep`, `orange*`, `purple`, `amber*`, `side-*`. Raio 8px, borda 1px, sombra só em modal/toast, gradiente só na sidebar, `tabular-nums` em números, `.stagger` para a entrada das páginas, `prefers-reduced-motion` respeitado.

## 9. Pendências e limites conhecidos

- **Indeed é frágil por natureza.** Ele bloqueia navegador oculto e desafia o visível depois de poucas cargas. O adapter respeita isso (ver histórico de 21/09/2026); se a pessoa não estiver por perto para resolver a verificação na janela, a varredura do Indeed falha e se pausa — é o comportamento correto, não um bug a "consertar" com técnicas de evasão. A candidatura no Indeed (`core/platforms/indeed/index.ts → candidatar`) nunca rodou contra o site real: trate a primeira execução em ensaio como o teste dela.
- **Questionário sequencial do InHire (form-app.inhire.app) não pôde ser observado:** em 18/09/2026 o app estava quebrado no servidor do InHire (asset JS servido como HTML), então o iframe fica em branco para todo mundo. O motor genérico foi escrito pelas heurísticas do prompt e validado contra o Typeform embutido; quando o form-app voltar, a primeira candidatura real numa empresa com fluxo condicional (Loggi, Conta Azul, Alelo) é o teste que falta — o ensaio nessas empresas já percorre o caminho e devolve "questionário não carregou" enquanto o app estiver fora.
- **O que acontece depois de "Continuar inscrição" não foi testado ao vivo** (criaria candidaturas reais). Verificado em ensaio (rotas de envio abortadas): as duas abas do InHire em Radix, DB1, Conta Azul e Loggi (país/cidade, CPF, rádios, diversidade, aceite, botão final liberado) e o motor Typeform nos formulários públicos da Radix, Conta Azul e Loggi (todas as perguntas respondidas até o botão Enviar). Não verificado: o iframe do Typeform dentro da página do InHire após criar o talento, e o "conditional flow" (empresas com `publicCapabilities: requireCustomFormCompletion`, ex.: Loggi, em que o InHire renderiza as perguntas do Typeform nativamente e cria o talento só no fim, via `POST /forms/form/submit`) — o laço genérico deve cobrir, mas vale conferir a primeira candidatura real olhando o log.
- **reCAPTCHA Enterprise** (invisível, por pontuação) protege `POST /job-talents/public/<jobId>/talents`. Se o InHire desafiar, o fluxo marca erro "verificação (captcha)" e não finge sucesso.
- **Dropdowns de diversidade** mostram título + descrição colados no DOM; as opções são lidas pelo primeiro texto e casadas por `melhorOpcao` (igual → prefixo → contém → similaridade ≥ 0,6).
- **DOCX** é aceito no upload mas não é lido; só PDF com texto gera perfil de busca.
- **Perguntas extras com upload de arquivo** são ignoradas (só o currículo é anexado).
- **E-mail de confirmação** não é enviado; a confirmação é log + registro + aviso na interface.
- **Score** é léxico (dicionário de ~90 competências + cargos). Sem embeddings.
- `database/` é o schema Supabase de uma fase anterior; o app local não o usa.
- Tauri: ver §3.

## 10. Verificações

- `npm run check` — auto-verificação do núcleo (roda em ~10 s, abre o Edge headless para o PDF).
- `npm run build` — tipos do front (`tsc`), do núcleo (`tsc -p core`) e build do Vite.
- Ponta a ponta manual: `start.bat`, cadastrar com um PDF de texto, conectar `vagasbyintera`, buscar, candidatar em ensaio, abrir a captura.

## 11. Histórico

| Data | O que foi feito |
|---|---|
| 11/09/2026 | Front completo a partir dos mockups; depois limpeza dos mockups. Commits `d088330`, `fa33d76` |
| 14/09/2026 | Schema Supabase em `database/`; `agentlog.md`; redesign escuro testado e desfeito (ficaram só as transições). Commit `1f9de5b` |
| 17/09/2026 | App local funcional com `start.bat`, cadastro inicial e estado no `localStorage`; mocks removidos |
| 21/09/2026 | **Sem Piedade provado ponta a ponta, e um defeito que ele mesmo revelou.** Primeira execução real do ciclo completo (em ensaio): vaga pausa → IA responde → vaga volta à fila → formulário repreenchido → concluído. Funcionou: "Em qual Estado você reside?" → *Ceará (CE)*; "Quanto tempo de experiência com tecnologia?" → *3 a 5 anos*; "Já teve vínculo com a Nomad?" → *Nunca tive* (gravada só para aquela vaga, por ser específica da empresa); "Qual sua remuneração atual?" → recusada e devolvida ao usuário; grupos de diversidade → não foi para a IA, com a mensagem certa. **Defeito encontrado:** "Qual seu nível de domínio em APIs REST?" recebeu *"Nunca utilizei ou só conheço conceitualmente"* — as instruções de responder o mínimo e o tom de "ainda estou aprendendo" vazaram para escala de domínio sobre tecnologia que o currículo comprova, subestimando o candidato diante do recrutador. O prompt passa a separar os dois casos: modéstia só para o que falta no currículo; havendo evidência, escolher a opção que a experiência real sustenta. Reverificado: REST, React e Docker → *Utilizo com autonomia*; COBOL e Salesforce → *Nunca utilizei*; tempo de carreira → *3 a 5 anos*; Kubernetes em texto livre → *Ainda não usei em projeto, estou estudando*. |
| 21/09/2026 | **Uma fonte de verdade por campo + `CLAUDE.md` + listas com "ver mais".** Auditoria de `ConfigAutomacao` rastreando cada campo até quem lê. **Campos mortos removidos** (apareciam na UI e o núcleo nunca lia): `salarioMin`/`salarioMax` — a faixa salarial era dois sliders que não filtravam nada —, `plataformas` (espelho de `conexoes`, que é quem o core usa), `local` (o score usa `perfil.cidade`) e `curriculo`. Saíram do tipo, da UI e o CSS `.dual-range` junto. **Duplicação de editor resolvida:** área, cargo e senioridade eram editáveis em Automação › Filtros **e** em Configurações › Perfil profissional — salvar num lado sobrescrevia o outro em silêncio. Agora o perfil profissional se edita só em Configurações e a Automação mostra um espelho com link; `cargo` deixa de existir em `automacao` e passa a vir de `perfil.cargo` (era copiado entre os dois). **`ler.automacao()` passa a devolver só as chaves de `AUTOMACAO_PADRAO`:** campo removido continuava gravado no blob e reaparecia no front como se ainda valesse. **`CLAUDE.md`** criado: fluxo, invariantes (sucesso = resposta HTTP, nunca inventar no currículo, autodeclaração fora de IA/similaridade, uma candidatura por vaga, ensaio não envia), a tabela de donos de cada dado e as decisões de score que custaram caro. **UI:** `src/components/VerMais.tsx` — vagas encontradas e fila de envio mostram 5 e escondem o resto atrás de "Ver mais N de M", com "ver todas" e volta ao tamanho curto; o log já tinha altura fixa e o Painel já paginava. |
| 21/09/2026 | **Publicação repetida da mesma vaga: trava por empresa + título.** A fila real mostrou a Radix duas vezes com "Profissional Desenvolvedor de Software Pleno" — `jobId` diferentes no InHire, mesma vaga para o recrutador, e a trava existente só olhava `id`. Três currículos iguais chegando à mesma pessoa é pior do que não se candidatar. Nova `chaveDaVaga()` (empresa + título normalizados) e `jaCandidatado()` em candidatura.ts substituem `jaEnviada()` nas decisões: `executarCandidatura` recusa a publicação irmã de algo já enviado (e a **encerra**, para não voltar à fila), `candidatarAgora` recusa o clique manual com a razão, e `enfileirarCompativeis` passa a enfileirar **uma publicação por empresa+título** — a de maior compatibilidade; as irmãs ficam em `encontrada`, de reserva, caso a escolhida falhe. `limparDuplicatasDaFila()` cobre a fila montada antes da regra e roda também na subida do núcleo, já que com o robô pausado o enfileiramento (que também limpa) nem executa. 3 cenários novos em fila-check (45 verificações no total): só a mais compatível entra; a irmã de uma vaga já enviada nunca chega a abrir a plataforma; fila antiga é limpa mantendo a melhor. **Verificado na fila real:** a duplicata da Radix saiu, e as três da BIX ("Back End Pleno - Go", "Back End Pleno(a) - Python", "Front End Pleno(a)") permaneceram — são vagas distintas, não repetição. |
| 21/09/2026 | **Sem Piedade, ajustes de uso:** (1) stack ausente do currículo deixa de virar um "não" seco — o prompt passa a pedir o tom de quem está começando ("Ainda não usei em projeto, estou estudando."), sem jamais inventar experiência; em pergunta Sim/Não com lista, escolhe a opção negativa limpa, sem texto extra. Verificado com a IA real: Kubernetes, Rust e Salesforce (ausentes do CV) → "Ainda não usei em projeto, estou estudando."; Docker e React (presentes) → "Sim" e "Mais de 3 anos.". (2) O seletor de modo estava só dentro do formulário de configuração, abaixo da dobra — quem chega para ligar o robô não o via. Passa a existir também como faixa logo abaixo do status, com os dois modos em botões de alternância (aria-pressed, não um radiogroup falso) e a explicação do modo escolhido ao lado; a troca **grava na hora** em vez de esperar "Salvar configuração", senão daria para mudar o modo e ligar o robô com o anterior valendo. |
| 21/09/2026 | **Modo "Sem Piedade": a IA responde as perguntas das empresas e o envio roda 100% sozinho.** Novo `ConfigAutomacao.modoPerguntas` (`manual` \| `sem_piedade`, padrão manual) com seletor em Automação › Perguntas das empresas — a opção fica desabilitada sem IA configurada. **Arquitetura:** nada de tornar `responder` assíncrono e espalhar await pelo motor de formulário; o gancho é a pendência que já existe — a vaga cai em `aguardando_pergunta`, o trabalhador da fila chama a IA, grava a resposta e devolve a vaga para `na_fila`, reaproveitando o mesmo caminho testado do "usuário respondeu". Teto de 8 respostas por vaga (`respostasIA`, em memória) para formulário que não para de perguntar não virar laço. **`responderPergunta()` em ia.ts** recebe currículo + vaga + pergunta e devolve `string | null`; null pausa a vaga como no modo manual. O prompt exige resposta mínima (nunca mais de 2 frases), primeira pessoa, português de gente, e proíbe travessão, listas, emoji, aspas e o vocabulário que denuncia IA. Quatro filtros de saída: `PULAR` (o próprio modelo avisa que não dá para responder com honestidade), tamanho > 240, `CHEIRO_DE_IA` e, em pergunta de lista, a resposta tem de ser **exatamente** uma das opções. **Duas regras que não se negociam:** autodeclaração (gênero, cor/raça, PcD, religião, saúde) **nunca** vai para a IA — continua vindo só da política do usuário; e pergunta específica da empresa é gravada como `respostaTemporaria`, então não vira resposta padrão em outra vaga. **Testado com a IA real (Gemini) em 6 perguntas:** "Quantos anos com React?" → *"Mais de três anos."*; início imediato → *"Sim"*; Kubernetes (ausente do CV) → *"Não."*; e três recusadas corretamente — impressões sobre a empresa, nível de inglês (não está no CV) e "por que quer trabalhar conosco". A primeira rodada devolvia *"Não possuo informações suficientes"* e *"Não informado no currículo"* — respostas **sobre** o currículo, que entregam a máquina: viraram padrão recusado em `CHEIRO_DE_IA` e regra explícita no prompt. 42 verificações no self-check (2 novas na fila: sem IA configurada o modo pausa em vez de fingir; autodeclaração segue fora do alcance da IA). |
| 20/09/2026 | **Autodeclaração: escolher em vez de digitar; e "guardar resposta" deixa de vir marcada em pergunta da própria empresa.** (1) O modo "usar minhas respostas padrão" exigia digitar o texto **exatamente** como a vaga escreve ("Mulher Cisgênero"), o que praticamente garantia não casar — na prática ninguém usava e tudo pausava. `CategoriaSensivel` ganha `opcoesComuns` com as respostas como os formulários do InHire realmente escrevem (gênero, orientação, cor/raça, PcD, religião, saúde, grupos de diversidade; `PREFIRO_NAO_RESPONDER` em todas) e `ConfigSensiveis.tsx` troca o campo livre por um seletor — "Perguntar toda vez" é o padrão, e há "Outra resposta…" para o que fugir da lista. Os campos deixam de ficar escondidos atrás do modo: aparecem sempre, e escolher qualquer resposta já liga o modo que a usa (com aviso quando há resposta definida e o modo não a usa). O casamento com a opção da vaga segue por similaridade ≥ 0,7, então "Homem Cisgênero" acha "Homem cisgênero"; a regra de nunca responder por semelhança com OUTRA pergunta continua intacta. (2) A caixa "Guardar esta resposta para perguntas parecidas" nascia marcada mesmo em pergunta específica da empresa — "Quais são as suas impressões sobre as nossas produções?" respondida com "excelente" viraria a resposta padrão na vaga de outra empresa. Nova `perguntaSoDestaVaga()` em src/dados.ts detecta empresa-como-"nós" ("nossas produções", "conosco"), o nome da empresa no texto ("Como conheceu a QI Tech?"), "por que quer trabalhar…", "o que você sabe sobre…" e "esta vaga"; nesses casos a caixa nasce desmarcada com a explicação. Perguntas genéricas (inglês, CNH, pretensão, Scrum) seguem marcadas. 39 verificações no self-check. |
| 20/09/2026 | **Afinidade entre famílias, corte por localidade e senioridade acima.** Três pedidos do usuário, com efeito medido no acervo real. **(1) "Rigor rígido" não pode significar só a família exata:** quem é Full Stack quer ver Engenheiro de IA, QA e dados como alta relevância. Nova família `IA e Machine Learning` e `AFINIDADE` em analyzer.ts (4 grupos; o de tecnologia reúne Desenvolvimento, IA/ML, Qualidade e Testes, Dados e Analytics e Segurança da Informação) com `familiasAfins()`; `cargoParecido` passa a usar afinidade em vez de igualdade. Como os 40 pontos do título vinham de semelhança de texto — e "QA Automation Engineer" não se parece com "Desenvolvedor Full Stack" —, função reconhecida ganha piso no título (0,8 mesma família, 0,65 afim), senão o cargo relevante de nome diferente perdia quase tudo ali. **(2) Vaga um nível acima deixa de punir** (0,7 → 0,9): vaga sênior é alvo legítimo de quem é pleno. Dois ou mais acima continuam em 0,35 e dois abaixo em 0,5. **(3) Fora da sua cidade, só remotas:** `hibrido` não era nem checado (vaga híbrida em outro estado passava inteira) e agora exige presença como a presencial; com `presencialSoNaMinhaCidade` (novo em ConfigAutomacao, padrão true, com opção em Configurações › Perfil profissional) o que exige presença fora da sua cidade é **cortado** (score 0), não penalizado — não adianta listar vaga a que não se pode comparecer; local ilegível ("BR") continua valendo. **Bug que só apareceu com dado real:** `fatorLocal` exigia UF nos dois lados, então a cidade do perfil escrita sem UF ("Fortaleza") empatava com qualquer lugar do país e nada era cortado — agora cidades diferentes bastam. **Resultado:** de 219 compatíveis (83 delas presenciais/híbridas fora de Fortaleza) para 136 — 133 remotas e 3 híbridas, todas em Fortaleza, **zero** fora do alcance; e a abrangência que se queria continua: LLM Engineer 79%, DevOps Pleno 81%, Data Engineer 74%, QA Pleno 68%, AppSec 68%, FullStack Sênior 86%. Config do usuário: rigor rígido, score mínimo 50. 37 verificações no self-check. |
| 20/09/2026 | **Compatibilidade currículo × vaga: o filtro estava errado e foi refeito.** Apontamento do usuário ("tem vaga de Analista de Processos na fila, não combina com meu perfil") — e os dados deram razão: vaga de BPM/RPA com 75%. Quatro defeitos, todos com evidência no acervo real de 2.212 vagas. **(1) Senioridade do candidato lida como Estágio** num currículo de "Full Stack há mais de 3 anos" + "TechLeader": `analisarCurriculo` reusava `inferirSenioridade` (feita para VAGA, onde citar vários níveis significa o mais baixo) e o padrão de anos exigia literalmente "anos de experi", que "há mais de 3 anos" não casa. Novas `anosDeCarreira()` e `inferirSenioridadeDoCurriculo()` em analyzer.ts: vale o nível mais ALTO, "estágio" só decide sem nenhum outro sinal. **(2) Score premiava vaga vaga:** o denominador era a lista de skills da vaga, então "sql, rest, testes" dava 100% a qualquer pessoa de TI. Entra `pMinhas` — quanto do SEU núcleo técnico a vaga pede (`NUCLEO_STACK = 6`) — combinado 60/40 com a leitura antiga. **(3) Cargo diferente quase não pesava:** similaridade de bigramas não distingue profissão ("Desenvolvedor Full Stack há mais de 3 anos" × "Analista de Processos" = 0,42 só por " de "/"os"). Nova `FAMILIAS_CARGO` (15 famílias) e `familiaDoCargo()` decidem pela FUNÇÃO; texto só é usado quando nenhum lado tem família. `fatorCargo` multiplica 0,6 (equilibrado) ou 0,35 (`cargoRigido`, novo em ConfigAutomacao). Senioridade dois degraus ABAIXO também perde (0,5): Pleno não disputa estágio. **(4) A IA nunca reavaliava o acervo** — falhou com 429 na varredura e não havia como refazer. `avaliarVagas` ganha backoff de cota (3 tentativas, 5/20/60 s) e devolve o que conseguiu em vez de perder tudo; nova `repontuarComIA(limite)` + rota `POST /repontuar-ia` + botão "Reavaliar com IA" na Automação. **Perfil gravado ficava velho:** `migrarPerfilBusca()` no start recalcula o `perfilBusca` do currículo e dispara `repontuar()` quando muda; `repontuar()` passa a atualizar também fila/ensaio/erro (status preservado; quem cai abaixo do mínimo sai da fila). **Configurações › Meus Dados** ganha a seção "Perfil profissional" (senioridade, área, rigor na função) e marca com `*` os campos que decidem acurácia ou que o InHire exige. **Resultado medido no acervo real:** Estagiário de Novos Negócios 72% → 17%, Analista de Risco de Liquidez 73% → 35%, Analista de Processos 75% → 38%, Analista de Fundos 86% → 43%; e vagas Full Stack Pleno que estavam **ignoradas com ~30%** subiram para 88–95%, ocupando todo o topo. 6 verificações novas em self-check (35 no total). |
| 20/09/2026 | **A fila passa a nascer cheia: `enfileirarCompativeis()`.** Até aqui só vaga recém-descoberta entrava na fila (`buscarVagas` percorria apenas `novas`), então ligar o modo automático com 178 vagas já encontradas não fazia absolutamente nada — a fila nascia vazia e só uma varredura futura a alimentaria. Provável causa principal do relato "o robô não candidata sozinho". Agora uma função única em queue.ts põe na fila as vagas **já encontradas** que passam nos filtros atuais (status `encontrada`, score ≥ `scoreMinimo`, modelo dentro de `regimes` — `indefinido` passa —, e nunca uma `jaEnviada`), ordenadas da mais compatível para a menos, limitada ao que **ainda cabe no limite diário** (`limiteDiario − enviadas hoje − já na fila`): a fila fica legível, o limite é respeitado na origem e o resto entra sozinho no dia seguinte. Chamada em quatro pontos: varredura, `ligarRobo(true)`, `POST /estado` quando muda modo/filtros/regimes/limite (salvar configuração passa a ter efeito imediato) e no laço de 20 s **apenas quando a fila está vazia** (a checagem barata é `proximaNaFila()`; o levantamento completo só roda quando ela não acha nada). 4 cenários novos em `fila-check.ts` (13 no total): ligar o robô enfileira as já encontradas respeitando score mínimo e filtro de regime; só o que cabe no limite diário, por ordem de compatibilidade; modo manual e robô pausado não enfileiram; vaga já enviada nunca volta. **Verificado ao vivo** (ensaio, janela 00:00-23:59, intervalo 2 min, limite 8): ligar o robô enfileirou 7 na hora, a primeira rodou em seguida e, no intervalo seguinte, o trabalhador percorreu quatro vagas seguidas — três pausaram em pergunta da empresa ("Como conheceu a QI Tech?", CPF em campo livre, "Você já conhece a Brasil Paralelo?") **sem travar a fila** e a quarta completou o ensaio. |
| 20/09/2026 | **PRIMEIRA CANDIDATURA REAL enviada + Biome.** Envio real na BIX Tecnologia ("Desenvolvedor(a) Full Stack (C# + React)", remota, currículo original, regime PJ): `POST /job-talents/public/<id>/talents` respondeu **201** e a candidatura foi confirmada. O caso que se materializou foi exatamente o da correção de 19/09: logo após o 201 o InHire embutiu o questionário `form-app.inhire.app`, que **não carregou** (asset JS servido como text/html — quebra do servidor deles) — o código antigo teria marcado `erro` e o usuário reenviaria, gerando candidatura duplicada; o novo registrou "a tela não confirmou, mas a candidatura foi aceita pelo InHire" e gravou `enviada`. A trava de duplicidade foi verificada ao vivo: candidatar de novo devolve `você já se candidatou a esta vaga`. **Defeito encontrado na preparação:** `workModel` respondia **"Sim" fixo** a "tem disponibilidade para trabalhar no modelo presencial em <cidade>?" — declararia, em nome do usuário e para um empregador real, disponibilidade inexistente (vaga da QI Tech presencial em São Paulo/Pinheiros com o perfil em Fortaleza). `disponibilidadeNoModelo` em formulario.ts passa a afirmar "Sim" só quando a pergunta não envolve presença física ou quando o local sai do rótulo e `fatorLocal` dá mesma cidade/estado; caso contrário pausa e pergunta. **Biome 2.5.14** configurado (`biome.json` alinhado ao estilo do repositório: 2 espaços, aspas simples, 200 colunas, CRLF, `tailwindDirectives`), scripts `lint`/`lint:fix`/`format`, `biome check` no `build`, 47 arquivos formatados e os 8 achados de lint resolvidos — `!important` do `prefers-reduced-motion` preservado com `biome-ignore` justificado (removê-lo quebraria a acessibilidade). |
| 20/09/2026 | **Validação do envio autônomo (testes executáveis + ensaio em 20 empresas reais):** dois verificadores novos entram no `npm run check`. `core/fila-check.ts` roda a fila contra um adapter falso e banco temporário (9 cenários: encadeamento, portões de agendamento, pedido manual, pendência, nova tentativa, desistência, duplicidade, ensaio, currículo ausente). `core/envio-check.ts` sobe um InHire falso local e prova as três decisões do motor: tela confirma → enviada; **API aceita mas a tela muda a redação → enviada mesmo assim** (o bug que fazia o robô marcar erro e reenviar); HTTP 400 → erro com o motivo do servidor; ensaio → 0 POST. **Dois defeitos achados e corrigidos pelos testes:** (1) nada impedia candidatar DUAS VEZES na mesma vaga — `jaEnviada()` em candidatura.ts agora barra em qualquer caminho (fila, clique manual, retomada de pendência) e `candidatarAgora` recusa na entrada; (2) o texto do botão final muda por empresa — `BOTAO_FINAL` só conhecia "Continuar inscrição" e falhava em "Candidatar-se para a vaga" (Ninecon, Insider Store), e `acharBotao` passa a procurar dentro do `<form>` primeiro (o "Candidatar" do topo só rola a página) e a aceitar o `<div role=button>` que embrulha o botão real. **Ensaio em 20 empresas distintas depois das correções: 14 prontas para envio, 4 pausadas em autodeclaração (correto — dado sensível nunca é adivinhado), 2 com o `form-app.inhire.app` fora do ar (asset JS servido como text/html no servidor do InHire; confirmado em 20/09). Nenhuma falha atribuível ao motor.** |
| 20/09/2026 | **10 plataformas reservadas no catálogo** (lista trazida pelo Erich): RemoteOK, Remotive, Wellfound, Working Nomads, JS Remotely, PowerToFly, FlexJobs, Toptal, Job Hunt e Kickresume entram em `src/dados.ts` com `disponivel: false` e os campos novos `site` e `nota` (`Plataforma` em src/types.ts) — o que já se sabe de cada uma (API pública, exige conta, assinatura paga, ou nem é mural de vagas) fica registrado para a pesquisa de viabilidade não recomeçar do zero. O card de Plataformas passa a mostrar essa nota e um link para o site em vez do genérico "Integração ainda não implementada". Nenhum adapter novo: o núcleo continua só com o InHire. Candidatas mais próximas de virar adapter: **RemoteOK** e **Remotive** (feed público em JSON). Sem caminho de automação: **Toptal** (processo seletivo próprio), **Job Hunt** (portal de conteúdo) e **Kickresume** (montador de currículo) |
| 19/09/2026 | **Confiabilidade do envio (o robô enviava e reportava erro):** a confirmação deixou de depender só do texto da tela. `index.ts` escuta as respostas HTTP das rotas que criam a candidatura (`ROTAS_ENVIO` em selectors.ts: `/job-talents/public/<id>/talents`, `/forms/form/submit`) — 2xx é **prova dura de envio**: nenhum desvio de layout depois disso vira erro, exceção pós-envio conta como enviada, e o botão final nunca é clicado duas vezes (trava anti-duplicidade). 4xx/5xx viram o motivo real devolvido pelo InHire em vez de "sem confirmação". `SUCESSO` ampliado (registrada, finalizada, "em breve entraremos", "recebemos seu currículo"…). **Fila que não andava:** `processarProxima` virou um trabalhador que processa uma candidatura atrás da outra até um portão fechar (intervalo, limite diário, janela, robô pausado), com flag `rodando` separada de `ocupado` (o laço de 20 s entrava entre duas candidaturas e abria fila paralela) e `pedidoForcado` (resposta/aprovação do usuário durante uma candidatura não era perdida — antes, em modo manual, a vaga ficava parada para sempre). **Nova tentativa:** `core/falhas.ts` classifica a falha (transitória × permanente); transitória volta para a fila com espera 2/10/30 min (`Vaga.tentativas`, `proximaTentativaEm`, respeitados por `proximaNaFila`), permanente (captcha, vaga encerrada, recusa do servidor) não se repete. **Diagnóstico:** botão final travado agora diz qual campo obrigatório ficou vazio; `navegador()` detecta contexto morto e recria (antes, um Edge fechado travava todas as candidaturas seguintes); log explica por que a fila está parada e avisa em voz alta quando o modo ensaio está ligado |
| 21/09/2026 | **Firefox como navegador do robô (opcional).** `automacao.navegador: 'edge' \| 'firefox'` (Automação › Segurança). `core/browser.ts`: Firefox = build do Playwright (`firefox.launchPersistentContext`, perfil `<navegador>-firefox`; o Playwright NÃO dirige o Firefox do sistema), com queda para o Edge + alerta no log se a build não existir; trocar de motor fecha e reabre a sessão. `comNavegadorDePdf`: o PDF passou a sair sempre de um Chromium oculto à parte, porque `page.pdf()` não existe no Firefox **nem no Edge com janela visível** (isso corrigiu um bug latente: "Mostrar o navegador" quebrava a geração do PDF adaptado). Nesta máquina `npx playwright install firefox` falha por rede; a build 1543 foi baixada com curl retomável e descompactada em `%LOCALAPPDATA%\ms-playwright\firefox-1543` + marcador `INSTALLATION_COMPLETE`. Verificado em ensaio com Firefox 155: Radix (2 etapas, 18 campos) e DB1 (12 campos) prontos, anexo por filechooser ok, PDF ok; ~5× mais lento que o Edge |
| 21/09/2026 | **Localização multi-país + adapter do Indeed.** (B) `core/localizacao.ts` é a regra central (`vagaCompativelComLocalizacao`, `paisDoLocal`, `lerLocal`): presencial **e híbrida** comparam com `perfil.cidade` (mesma cidade ok; outra cidade do estado fator 0,6; outro estado/país incompatível = score 0); remota compara `vaga.pais` com `perfil.paisesRemoto` (padrão `['Brasil']`; remota sem país declarado é compatível). `src/paises.ts` tem países + domínio do Indeed; `Vaga.pais`; `ler.localizacao()`; a IA não devolve à lista vaga incompatível; `SCORE_VERSAO` 4 repontua tudo ao subir. UI: Configurações › Meus Dados › "Onde você aceita trabalhar" (cidade + multi-select de países com tags, datalist nativo). (A) `core/platforms/indeed/` (`seletores.ts`, `busca.ts`, `index.ts`): só vagas com `indeedApplyEnabled` ("Candidatar-se facilmente"), lidas do JSON embutido `window.mosaic…mosaicProviderJobCardsModel.results` (DOM como reserva); presenciais no domínio do país da pessoa com a cidade, remotas em cada país de `paisesRemoto` com `sc=0kf:attr(DSQF7);` (DSQF7 = Home Office e PAXZC = Modelo Híbrido, confirmados nos atributos da página); id `indeed:<jobkey>` deduplica entre domínios. **Limites reais, confirmados ao vivo:** navegador headless leva 403 "Solicitação bloqueada"; o Edge visível e sem alteração leva 200, mas o Indeed passa a exigir "Verificação adicional" (Cloudflare) depois de poucas cargas. Por isso: janela sempre visível, 30 s entre cargas, no máximo 2 cargos, 1 varredura automática/dia, e diante de bloqueio/verificação o robô espera 3 min pela pessoa e se pausa 12 h — **não há e não deve haver disfarce de automação, troca de impressão digital nem resolução de captcha**. Login manual na janela do robô (`POST /indeed/entrar`; o AutoCV não vê a senha; cookies no perfil do navegador). Motor de formulário ganhou `OpcoesFormulario.convencoes` (botões/sucesso por plataforma). Verificado: busca em Fortaleza (15 resultados → 7 com candidatura simplificada, 8 externas descartadas, híbrida reconhecida). **Não verificado:** busca em pt.indeed.com (a 2ª carga do teste caiu na verificação), domínios além de br, parâmetro de URL do filtro "Candidatar-se facilmente" (o modelo tem `indeedApplyOnlyFilterUsed`, mas nenhum link revela o parâmetro; filtramos por vaga, sem custo), login e todo o fluxo de candidatura do Indeed (exigem conta e criariam candidaturas reais) |
| 18/09/2026 | **Combobox rico + política para autodeclaração:** (1) dropdowns com título + descrição por opção (`react-dropdown-select` da diversidade; também `[aria-haspopup=listbox/menu]`) viram `OpcaoDropdown {textoPrincipal, textoSecundario}` em `formulario.ts` — casamento só pelo título, log com a descrição; rótulo pela heurística do elemento anterior quando não há aria-label. (2) `src/sensiveis.ts` (compartilhado front/núcleo): categorias versionadas por palavra-chave (gênero, orientação, raça/cor, PcD, religião, saúde, grupos de diversidade), `decidirSensivel` puro — nunca por similaridade: resposta literal → padrão por categoria (modo "padrao") → "Prefiro não responder" (modo "prefiro_nao", só se opcional e a opção existir) → pausa sinalizada como dado sensível (`PerguntaExtra.sensivel`, modal avisa). Nova aba Configurações › Autodeclaração e dados sensíveis (`ConfigSensiveis.tsx`, kv `sensiveis`). `PerguntaExtra.obrigatoria` passa a viajar do DOM/schema até a política. Nenhuma inferência a partir de nome/foto/currículo. Schema tolera formId UUID (formulário nativo do InHire, sem definição pública). Verificado em ensaio na Radix: parecidas não reaproveitadas, opcionais com "Prefiro não responder", obrigatória pausa como sensível, literal completa o formulário |
| 18/09/2026 | **Modo sequencial (uma pergunta por tela):** Etapa 0 confirmou que o questionário chega como **iframe do próprio InHire** (`form-app.inhire.app/form?jobId&formId=<typeformId>&type=subscription`, com `talentId` depois de criar o talento ou `flow=returnMessageToParent` no fluxo condicional das empresas com `publicCapabilities: requireCustomFormCompletion` — Loggi, Conta Azul, Alelo — em que o questionário vem ANTES do talento); em `/forms/preview/<id>` é o widget do Typeform. `detectarModo` roda a cada etapa (frames + página nativa); `preencherSequencial` escolhe o motor Typeform (data-qa) ou o genérico (`preencherSequencialGenerico`: pergunta em destaque, controles visíveis, Enter → botão próximo, tela final, limite 50). Ensaio clica o botão final só no fluxo condicional (abre o questionário sem criar nada; POSTs abortados). **Limite:** no dia do teste o form-app estava quebrado (o `index.html` dele aponta para um `.js` que devolve HTML), então o motor genérico foi validado numa página-mãe local com o Typeform embutido em iframe (Radix 4 perguntas, Conta Azul 9 com múltipla escolha, Loggi 4: detecção → boas-vindas → respostas → botão Enviar, em ensaio) e o caminho real da Loggi devolve o erro específico "questionário não carregou". Regras que fizeram diferença: ignorar `[inert]`/`aria-hidden` (telas vizinhas ficam no DOM), clicar no título antes do Enter (foco no iframe), "OK" com dica colada |
| 18/09/2026 | **Motor de formulário adaptativo (InHire):** Etapa 0 confirmou schema via API (`job-posts/public/pages/<id>` → `settings.fields/requiredFields` + `diversity.questions`; `forms/public/job-id/<id>/subscription` → `typeformId`; `form.typeform.com/forms/<id>` público) → `schema.ts` + `perguntasPrevias` no adapter (pausa antes de abrir o navegador). `formulario.ts` substitui o fluxo fixo: descoberta de campos por etapa, classificação fixo × extra, preenchimento por tipo (inclui `react-dropdown-select` de país/cidade e grupos de checkbox), navegação Avançar → Continuar inscrição, motor Typeform por bloco ativo. Novos: `PerguntaExtra.tipo = 'multipla'` (resposta "A \| B", modal com checkboxes), CPF e cidade no perfil (`PERGUNTA_CPF`/`PERGUNTA_CIDADE` vão para o perfil), tabela `formularios` + `vaga.formulario`, ensaio aborta as rotas de envio no navegador. Ensaio OK em Radix, DB1, Conta Azul e Loggi; Typeform OK em 3 formulários públicos |
| 18/09/2026 | **Local no score (só presencial):** `lerLocal` (cidade + UF a partir de "Cidade - UF", "Cidade, Estado", nome de estado sozinho) e `fatorLocal` em score.ts: vaga presencial na mesma cidade 1, outra cidade do mesmo estado 0,6, outro estado 0,2; remoto/híbrido ignoram. Cidade do candidato = `perfil.cidade` (Configurações → Dados); mudar ela repontua. `SCORE_VERSAO` = 3. Máscaras de telefone e pretensão em `src/mascaras.ts` |
| 18/09/2026 | **Senioridade no score:** `inferirSenioridade` (título manda, nível mais baixo quando cita vários; fallback "N anos de experiência"), fator 0,7 um nível acima / 0,35 dois ou mais acima da senioridade do candidato, seletor de senioridade na Automação (padrão = a do currículo), `repontuar()` quando os filtros mudam e uma vez por `SCORE_VERSAO` ao subir o núcleo, badge de nível nas vagas, senioridade informada à IA |
| 18/09/2026 | **Módulo de descoberta InHire:** tabela `empresas_inhire`, seed de 29 empresas verificadas, varredura agendada (Fonte A) com encerramento de vagas sumidas, Fonte B via Common Crawl (sem chave; Google opcional) — teste de 18/09 achou 75 empresas novas sozinho, UI de empresas monitoradas em Plataformas, aba Descoberta em Configurações, indicador no Painel. `automacao.tenants` migrado para a tabela |
| 18/09/2026 | Aba **Inteligência Artificial** em Configurações: provedor (nenhum / Gemini / Anthropic), modelo e chave; `core/ia.ts`; adaptação por IA com `validarEntidades`. Rotas `/ia` e `/ia/testar` testadas com chaves falsas (erros amigáveis); chamada real não testada por falta de chave |
| 18/09/2026 | **Módulo de automação InHire:** núcleo Node (`core/`) com `PlatformAdapter`, API pública do InHire, Playwright no Edge, SQLite, PDF ⇄ Markdown, perfil de busca, score, adaptação sem invenção + validador, fila com modo automático/manual, perguntas extras e preview como pendências, modo ensaio. Estado migrou do `localStorage` para o núcleo. Verificado ponta a ponta em ensaio contra o InHire real |

**Convenções:** textos em PT-BR; commits pequenos; `npm run build` e `npm run check` antes de commitar; nada de dependência nova sem necessidade real; nunca simular sucesso de envio; nunca deixar o adaptador de currículo introduzir informação.
| 21/09/2026 | **Vagas PJ integrado e provado em ensaio no site real.** Lista pelo `/feed-vagas.xml` + JSON-LD `JobPosting` de cada página (a busca do site é filtrada no cliente e a descrição do feed vem truncada; só 2 de 50 títulos diziam o modelo). Candidatura pelo motor de formulário do InHire, com as convenções daqui: o botão de envio é **"Candidatar"** exato — a regex do InHire exige "candidatar-se", e "Candidatar agora" apenas revela o formulário. **Três armadilhas do site, todas tratadas:** (1) `vagas-whatsapp-sale.js` intercepta o submit e abre um anúncio — o POST só sai no clique em "Continuar candidatura", daí o gancho `aposBotaoFinal`; se a validação do navegador falha, o clique não faz *nada*, então o gancho acusa o campo inválido em vez de esperar uma tela que nunca muda; (2) `custom-select.js` esconde o `<select>` real atrás de um botão — o motor passou a ler o controle nativo por trás do widget, senão o tipo de CNPJ virava pergunta anônima e *opcional*, com rótulo "Tipo do CNPJ (MEI ou ME) MEI ME Não tenho"; (3) `cv-upload.js` apaga o PDF do campo com um `alert()` se passar de 5 MB — conferido antes de abrir o navegador. **Bug achado de passagem:** `executarFormulario` chamava `acharBotao(page)` sem repassar as `Convencoes`, então Indeed e Vagas PJ procuravam os botões do InHire. **Prova de envio** (invariante 1): aqui o POST é de página inteira e o Laravel responde com redirect — 2xx/3xx vale como aceite, exceto redirect de volta para a própria vaga, que é `back()->withErrors()`. POST que sai sem resposta não volta sozinho para a fila (seria um segundo envio). Ensaio no site real: 6 campos preenchidos, PDF anexado, LinkedIn normalizado para URL completa (o campo é `type=url` com `pattern`), botão liberado, nada enviado. Busca real: 50 vagas, 19 remotas / 6 híbridas / 25 presenciais, 0 indefinidas; a de Full Stack Pleno em Fortaleza pontua 79. 50 verificações. |
| 21/09/2026 | **Catálogo de plataformas podado de 18 para 8, por um critério só.** O que decide se dá para automatizar não é login, paywall nem anti-robô — é se **a candidatura acontece dentro da plataforma**. Site que redireciona para o formulário da empresa não tem "um adapter": tem um adapter por empresa, para sempre. Saíram: agregadores que redirecionam (Remotive, Working Nomads, JS Remotely, Glassdoor); assinatura paga só para candidatar (Catho, FlexJobs); candidatura "com o perfil" em vez de PDF, que automatizar não economiza nada (Wellfound, PowerToFly); e o que nem é mural de vaga (Toptal é processo seletivo próprio, Job Hunt é conteúdo de carreira, Kickresume é montador de currículo) — com eles foi embora a região `ferramenta`. **LinkedIn saiu apesar de passar no critério**: a "Candidatura simplificada" é dentro da plataforma, mas a plataforma caça automação e o que se arrisca ali é a conta profissional do usuário. Ficaram 8: InHire, Vagas PJ e Indeed (prontas), Gupy como próxima prioridade clara (maior fatia das vagas de TI no Brasil, candidatura interna, conta única reaproveitada), Vagas.com e InfoJobs em seguida, e Trampos.co e RemoteOK marcadas **"a confirmar"** — a listagem delas é fácil, mas ninguém checou ainda se a candidatura é interna; se redirecionar, saem também. O porquê da poda ficou no cabeçalho de `src/dados.ts`, e os removidos ficam no histórico do git. |
| 21/09/2026 | **Vaga enviada sai da tela principal, e dá para escolher por quais portais o robô envia.** (1) "Vagas encontradas" escondia o trabalho que ainda falta atrás de currículos já enviados: `enviada` saiu da lista, com um botão para reexibir e um atalho para o histórico completo, que já existia no Painel ("Últimas candidaturas") — não foi criada tela nova. (2) Filtro por portal: `Conexao.enviar` (ausente = true, então conexão antiga continua enviando). A plataforma segue sendo varrida e as vagas continuam aparecendo; só não entram na fila. O campo mora na **própria conexão**, não em `automacao`, porque `automacao.plataformas` já foi removido uma vez justamente por ser um espelho que divergia de `conexoes`. **Decisão:** o filtro vale só para a fila automática — clicar em "Candidatar" numa vaga é ato seu, e um filtro do robô não manda em você; há cenário de teste para isso. Quando o filtro é o único motivo de a fila ficar vazia, o log diz isso, senão vira "liguei o robô e não acontece nada". De quebra: os cartões de plataforma da Automação mostravam "N empresa(s) monitoradas" para todas, número que só existe no InHire. 51 verificações. |
